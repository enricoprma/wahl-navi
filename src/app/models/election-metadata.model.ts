/** Describes the generated election dataset shown by the application. */
export interface ElectionMetadata {
  datasetId: string;
  appTitle: string;
  location: string;
  electionTitle: string;
  disclaimer: string;
}
